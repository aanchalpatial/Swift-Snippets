// Note: Remember removing initial view controller array from storyboard

let window = UIWindow(windowScene: scene)
window.rootViewController = TabBarViewController()
self.window = window
window.makeKeyAndVisible()
